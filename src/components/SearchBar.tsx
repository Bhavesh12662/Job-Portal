import { MapPin, Search, BriefcaseBusiness, IndianRupee } from 'lucide-react'; import { useNavigate } from 'react-router-dom'; import { useAppStore } from '../store/useAppStore';
export function SearchBar({compact=false}:{compact?:boolean}){const {filters,setFilters}=useAppStore();const navigate=useNavigate();const submit=(e:React.FormEvent)=>{e.preventDefault();navigate('/jobs')};return <form className={`searchbar ${compact?'compact':''}`} onSubmit={submit}><label><Search size={18}/><input value={filters.keyword} onChange={e=>setFilters({keyword:e.target.value})} placeholder="Skills, designation or company"/></label><label><MapPin size={18}/><input value={filters.location} onChange={e=>setFilters({location:e.target.value})} placeholder="Location"/></label><label><BriefcaseBusiness size={18}/><select value={filters.experience} onChange={e=>setFilters({experience:e.target.value})}><option value="">Experience</option><option>0–1 years</option><option>1–3 years</option><option>3–5 years</option><option>5+ years</option></select></label><label className="salary"><IndianRupee size={18}/><select value={filters.salary} onChange={e=>setFilters({salary:e.target.value})}><option value="">Salary</option><option>0–8 LPA</option><option>8–15 LPA</option><option>15–25 LPA</option><option>25+ LPA</option></select></label><button className="primary">Search jobs</button></form>}
