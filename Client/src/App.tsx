import { Navigate, Route, Routes, useLocation } from 'react-router-dom'; import { Layout } from './components/Layout'; import { HomePage } from './pages/HomePage'; import { JobSearchPage } from './pages/JobSearchPage'; import { JobDetailPage } from './pages/JobDetailPage'; import { AdminPage, CandidateDashboard, RecruiterDashboard } from './pages/Dashboards'; import { AuthPage } from './pages/AuthPage'; import { useAppStore } from './store/useAppStore';
function Protected({role,children}:{role:'candidate'|'recruiter'|'admin';children:React.ReactNode}){const user=useAppStore(s=>s.user);const location=useLocation();return !user?<Navigate to="/auth" state={{from:location.pathname}} replace/>:user.role===role?<>{children}</>:<Navigate to="/" replace/>} export default function App(){return <Layout><Routes><Route path="/" element={<HomePage/>}/><Route path="/auth" element={<AuthPage/>}/><Route path="/jobs" element={<JobSearchPage/>}/><Route path="/jobs/:id" element={<JobDetailPage/>}/><Route path="/dashboard" element={<Protected role="candidate"><CandidateDashboard/></Protected>}/><Route path="/recruiter" element={<Protected role="recruiter"><RecruiterDashboard/></Protected>}/><Route path="/admin" element={<Protected role="admin"><AdminPage/></Protected>}/><Route path="*" element={<Navigate to="/"/>}/></Routes></Layout>}
